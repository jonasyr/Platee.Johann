<UserControl x:Class="Platee.Johann.UI.Views.ToastView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Platee.Johann.UI.ViewModels"
             xmlns:conv="clr-namespace:Platee.Johann.UI.Converters"
             MouseEnter="OnMouseEnter"
             MouseLeave="OnMouseLeave">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <conv:NullToVisibilityConverter x:Key="NullToVis"/>
        <!-- Leise Rolle (#97): × #595959 statt #AAAAAA (2,3:1), beim Überfahren grauer Grund. -->
        <Style x:Key="CloseButtonStyle" TargetType="Button" BasedOn="{StaticResource QuietButtonStyle}">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="4,0"/>
            <Setter Property="AutomationProperties.Name" Value="Meldung schließen"/>
        </Style>
        <!-- Link-Rolle (#97): #C0392B statt #E63123 (4,35:1), Unterstreichung wirkt jetzt. -->
        <Style x:Key="DetailsLinkStyle" TargetType="Button" BasedOn="{StaticResource LinkButtonStyle}">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>
    </UserControl.Resources>

    <Border Background="White"
            BorderBrush="#DDDDDD"
            BorderThickness="1"
            CornerRadius="6"
            Margin="0,0,0,6"
            MinWidth="260"
            MaxWidth="340"
            AutomationProperties.AutomationId="Toast.Item">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="2" BlurRadius="8" Opacity="0.15"/>
        </Border.Effect>
        <Grid>
            <!-- Left tone accent stripe -->
            <Rectangle Width="5" HorizontalAlignment="Left">
                <Rectangle.Style>
                    <Style TargetType="Rectangle">
                        <Setter Property="Fill" Value="#27AE60"/>
                        <Setter Property="RadiusX" Value="5"/>
                        <Setter Property="RadiusY" Value="5"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Tone}" Value="Warn">
                                <Setter Property="Fill" Value="#B76E00"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Tone}" Value="Error">
                                <Setter Property="Fill" Value="#E63123"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Rectangle.Style>
            </Rectangle>

            <!-- Content area -->
            <DockPanel Margin="14,8,8,8">
                <!-- Close button -->
                <Button DockPanel.Dock="Right"
                        Content="×"
                        Command="{Binding DismissCommand}"
                        Style="{StaticResource CloseButtonStyle}"
                        AutomationProperties.AutomationId="Toast.Dismiss"
                        VerticalAlignment="Top"/>

                <!-- Running spinner -->
                <ProgressBar DockPanel.Dock="Right"
                             IsIndeterminate="True"
                             Width="36" Height="4"
                             Foreground="#E63123"
                             VerticalAlignment="Center"
                             Margin="0,0,4,0"
                             Visibility="{Binding IsRunning, Converter={StaticResource BoolToVis}}"/>

                <!-- Text stack -->
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="{Binding Title}"
                               FontSize="12"
                               FontWeight="SemiBold"
                               Foreground="#333333"
                               TextWrapping="Wrap"/>
                    <TextBlock Text="{Binding Message}"
                               FontSize="11"
                               Foreground="#666666"
                               TextWrapping="Wrap"
                               Margin="0,2,0,0"
                               Visibility="{Binding Message, Converter={StaticResource NullToVis}}"/>
                    <!-- Error detail link — walks up to Window.DataContext (MainViewModel) -->
                    <Button Content="Details im Status-Log"
                            Margin="0,4,0,0"
                            Style="{StaticResource DetailsLinkStyle}"
                            AutomationProperties.AutomationId="Toast.Details"
                            Visibility="{Binding ShowDetailsLink, Converter={StaticResource BoolToVis}}"
                            Command="{Binding DataContext.OpenProcessDetailCommand,
                                     RelativeSource={RelativeSource AncestorType=Window}}"/>
                </StackPanel>
            </DockPanel>
        </Grid>
    </Border>
</UserControl>
